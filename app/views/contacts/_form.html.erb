<%= simple_form_for(@contact) do |f| %>

  <%= f.error_notification %>

  <div class="field">
    <%= f.input :name, placeholder: 'Name' %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>

<% end %>
